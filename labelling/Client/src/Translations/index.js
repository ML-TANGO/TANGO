import resources from "./resources"

export const config = {
  interpolation: { escapeValue: false }, // React already does escaping
  lng: "ko",
  //resources: { ko: { common: { dashboard: { title: "대쉬보드" } } } }
  resources
}
